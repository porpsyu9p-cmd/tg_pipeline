#!/bin/bash
cd "$(dirname "$0")/frontend"

echo "🚀 Frontend..."

# Всегда обновляем зависимости
echo "📦 Installing dependencies..."
npm install

echo "✨ http://localhost:3000"
npm start

